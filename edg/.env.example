# ───────────────────────────────────────────────────────────────
# 前端环境变量 — 复制为 .env 后使用（.env 不会提交到 git）
# 改动后需重启 Vite dev server 才生效
# ───────────────────────────────────────────────────────────────

# 后端 API 地址
#
# 本地开发（默认）：
#   留空或设为 http://localhost:3001
#
# 公网 Cloudflare Tunnel（用于演示 / Office 在线预览）：
#   启动 `cloudflared tunnel --url http://localhost:3001` 后
#   把终端输出的 https://xxx-xxx.trycloudflare.com 填到下面
#
# 生产部署：
#   填你的后端公网域名，如 https://api.yoursite.com
VITE_API_BASE=http://localhost:3001
